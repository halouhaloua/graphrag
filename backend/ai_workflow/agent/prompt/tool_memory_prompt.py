TOOL_MEMORY_ANALYSIS_PROMPT = """你是工具使用专家，负责从**成功的工具执行**中提取优秀的工具使用方式和最佳实践，帮助未来更有效地使用该工具。

## 重要前提
- 只从**执行成功的工具调用**中提取经验
- 失败的工具调用根据报错信息，提取正确的使用方式
- 只提取**经过验证的优秀使用方式**，而非一般性知识

## 工具执行信息
- 工具名称: {tool_name}
- 执行状态: {execution_status}
- 输入参数类型: {param_types}
- 用户原始需求: {user_query}
{context_info}

## 核心任务
从上述**成功的工具执行**中提取**优秀的工具使用方式和最佳实践**，包括：

### 1. 工具适用场景（必须提取）
- 该工具最擅长处理什么类型的任务
- 已验证的成功使用场景和典型用途
- 例如："适合通过YouTube URL提取视频字幕信息"、"适合搜索网络上的公开信息"

### 2. 参数使用最佳实践
- 已验证有效的参数组合方式
- 参数的正确格式和取值范围
- 例如："url参数必须是有效的YouTube链接；language参数可选，支持zh/en"

### 3. 成功使用模式
- 已验证的成功调用模式
- 高效的使用方法和技巧
- 例如："批量处理时建议使用异步调用"、"复杂查询时使用分页参数"

## 提取原则
- **成功验证**：只提取经过实际验证的成功使用方式
- **场景优先**：首先说明工具最擅长做什么
- **具体可操作**：每条规则都应该明确、可验证、可直接应用
- **优秀实践**：重点提取高效、可靠的使用方式
- **事实性陈述**：只陈述客观的成功使用规则，不做主观评价
- **去重优化**：避免重复历史指导中已有的内容

## 输出规范
1. 只从成功执行中提取2-4条优秀使用方式
2. 第一条必须是已验证的工具适用场景
3. 每条经验用分号分隔
4. 总长度严格控制在500字符以内
5. 只输出纯文本，不要标题、解释或markdown格式
6. 如果工具执行失败，直接返回空字符串

## 开始输出
请从该工具的成功执行中提取优秀的使用方式和最佳实践
"""
